---
ah_namespaces:
  - name: update_collection_test
    # company:
    # email:
    # avatar_url:
    # description: string
    # resources: 
    links:
      - name: "nothing"
        url: "https://github.com/ansible/awx"
    groups:
      - name: system:admins
        object_permissions:
          - "change_namespace"
          - "upload_to_namespace"